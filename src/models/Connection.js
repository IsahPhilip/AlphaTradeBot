'use strict';

/**
 * @typedef {Object} Connection
 * @property {string} connectionId
 * @property {number} userId
 * @property {Date} expiresAt
 * @property {'pending'|'completed'|'expired'|'cancelled'} status
 */

module.exports = {};
